class: UserVerification
table: user_verification
fields:
  userId: int
  emailVerified: bool, default=true
  createdAt: DateTime, default=now
  updatedAt: DateTime?
indexes:
  user_verification_user_unique:
    fields: userId
    unique: true
