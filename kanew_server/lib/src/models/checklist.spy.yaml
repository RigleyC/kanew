class: Checklist
table: checklist
fields:
  cardId: int, relation(parent=card)
  title: String
  rank: String # LexoRank para ordenação (conforme plan.md)
  createdAt: DateTime
  updatedAt: DateTime?
  deletedAt: DateTime?
