# Definição de Etiquetas (Labels) do Board
class: LabelDef
table: label_def
fields:
  # Primary Key - UuidValue using Serverpod 3.x pattern
  id: UuidValue?, defaultPersist=random_v7
  
  boardId: UuidValue, relation(parent=board)
  name: String
  colorHex: String
  deletedAt: DateTime?
