# Serverpod Auth Secrets Configuration
# Copy this file to passwords.yaml and fill in your secrets
#
# SECURITY: passwords.yaml is gitignored - never commit real secrets!

# ============================================
# Development Environment
# ============================================
development:
  # Database password (match docker-compose.yaml)
  database: 'I60_gY2FPdtF_SHs924x42ui-u6HlCM7'

  # Redis password (match docker-compose.yaml)
  redis: 'qwQHbFwBgFbmMJg4mt3z77ykZlH5xiyC'

  # JWT Token Manager secrets
  # Generate with: openssl rand -base64 64
  jwtRefreshTokenHashPepper: 'dev-jwt-refresh-pepper-change-in-production-0123456789abcdef'
  jwtHmacSha512PrivateKey: 'dev-jwt-hmac-key-change-in-production-0123456789abcdefghij'

  # Email Identity Provider secret
  # Generate with: openssl rand -base64 32
  emailSecretHashPepper: 'dev-email-pepper-change-me'

# ============================================
# Test Environment
# ============================================
test:
  # Database password (match docker-compose.yaml)
  database: '0Hu7cFxzKYeeoGuIRqkRoIEHVte-ELVJ'

  # Redis password (match docker-compose.yaml)
  redis: 'WEF7sbrhavyRTkoVqiZ5XjtuRlIfMtlr'

  # JWT Token Manager secrets
  jwtRefreshTokenHashPepper: 'test-jwt-refresh-pepper-change-in-production-0123456789abcdef'
  jwtHmacSha512PrivateKey: 'test-jwt-hmac-key-change-in-production-0123456789abcdefghij'

  # Email Identity Provider secret
  emailSecretHashPepper: 'test-email-pepper-change-me'

# ============================================
# Production Environment
# ============================================
production:
  # Use environment variables or secrets manager
  database: '%SERVERPOD_PASSWORD_database%'
  jwtRefreshTokenHashPepper: '%SERVERPOD_PASSWORD_jwtRefreshTokenHashPepper%'
  jwtHmacSha512PrivateKey: '%SERVERPOD_PASSWORD_jwtHmacSha512PrivateKey%'
  emailSecretHashPepper: '%SERVERPOD_PASSWORD_emailSecretHashPepper%'
